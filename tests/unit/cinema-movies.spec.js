describe("CinemaMovies.vue", () => {
  //
  it('renders search inputs for "Search by name" and "Search by genre"', () => {});

  //
  it("renders MovieDetailCard for each cinema movie", () => {});

  it("renders MovieDetailCards for filtered by movies in cinema", () => {});

  it('displays "View More" button', () => {});

  it('emits "load-more" event when "View More" button is clicked', async () => {});
});
