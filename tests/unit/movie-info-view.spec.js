describe("MovieInfo.vue", () => {
  it("displays movie name", () => {});

  it("displays movie image", () => {});

  it("displays genre name", () => {});

  it("displays release date", () => {});
  //

  it("displays movie description", () => {});
  //

  it("displays availability status", () => {});

  it("displays stream or cinema option", () => {});

  it('emits "add-to-watchlist" event when "Add To Watchlist" button is clicked', async () => {});

  it('emits "select-streaming" event when "Stream Now" button is clicked', async () => {});

  it('emits "select-cinema" event when "Buy Tickets" button is clicked', async () => {});
});
