describe("MyWatchList.vue", () => {
  it('renders search inputs for "Search by list" and "Search by genre"', () => {});

  it("renders MovieDetailCard for each watchlist movie", () => {});

  it("renders buttons for each watchlist movie", () => {});

  it('emits "edit-movie" event when "Edit" button is clicked', async () => {});

  it('emits "delete-movie" event when "Delete" button is clicked', async () => {});

  it('emits "move-movie" event when "Move" button is clicked', async () => {});
  //

  it("displays move-movie menu with list of available lists and a blank field to create a new list and a save button", () => {});

  it('emits "save-movie" event when "Save" button is clicked', async () => {});
  //

  it('displays "View More" button', () => {});

  it('emits "load-more" event when "View More" button is clicked', async () => {});
});
